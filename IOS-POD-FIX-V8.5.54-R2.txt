MOLECULOX iOS V8.5.54 R2 - COCOAPODS RESOLUTION FIX

Fixes Codemagic failure in Generate and prepare iOS project:
- Removes the stale Podfile.lock created before the Google auth subspec is added.
- Resolves the complete Podfile with `pod install --repo-update`.
- Avoids a second Capacitor dependency update and uses `npx cap copy ios` afterward.

The conflict was between a lock-pinned GTMSessionFetcher/Core 4.5.0 and the
GoogleSignIn dependency range that requires a compatible 3.x version.

No game assets, levels, UI, Firebase web logic, or authentication UI were changed
in this R2 package. It only corrects iOS dependency preparation.
