MOLECULOX V8.5.53 — FINAL CROSS-PLATFORM AUDIT

FIXED
1. Repaired 20 campaign levels whose stored starting hint coordinates no longer pointed to an atom after earlier puzzle rebalancing.
2. Added a defensive exact-hint fallback to the existing certified solution.
3. Replaced the heavy all-at-once web precache with a reliable core-shell cache; large audio and story assets are cached as used.
4. Cache cleanup now removes only old Moleculox caches, not unrelated caches on the same origin.
5. itch.io, Netlify and Android packages now share the same 8.5.53 source.

PRESERVED
- Firebase Google/email authentication and cloud-sync code.
- Android package com.whitewaystudio.moleculox.
- Codemagic workflow moleculox-android-google-play.
- Signing reference moleculox_upload_key and mac_mini_m2 machine.
- All 301 campaign boards, 832 daily boards and certified solution sequences.
- Current-level mechanic guide and Training Center content.

LIMITATIONS
- A real Google/email login and cloud-sync test requires an AAB installed through Google Play Closed Testing.
- A signed AAB requires the Codemagic keystore environment and cannot be produced by static source validation alone.
