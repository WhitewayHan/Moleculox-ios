MOLECULOX iOS V8.5.70 R28 — iPHONE-ONLY APP STORE FINAL VALIDATION

RESULT: PASS

PURPOSE
- Remove iPad support from the submitted binary so App Store Connect no longer requires 13-inch iPad screenshots.
- Preserve the approved Moleculox icon fix and all existing R25 gameplay/release behavior.

CHANGES
1. Every Moleculox App target build configuration is forced to:
   TARGETED_DEVICE_FAMILY = 1
2. Stale UISupportedInterfaceOrientations~ipad metadata is removed from generated Info.plist.
3. Codemagic validates the generated Xcode project before building:
   - all Moleculox app configurations must be strictly iPhone-only.
4. Codemagic validates the actual signed IPA after building:
   - built Info.plist UIDeviceFamily must be exactly [1].
   - bundle identifier must be com.whitewayhan.moleculox.
5. R27 AppIcon generation and validation remain enabled:
   - opaque RGB 1024x1024 approved master.
   - modern universal and legacy AppIcon catalogs supported.
   - missing, transparent, wrong-size or mismatched icon stops the build.

PRESERVED BYTE-FOR-BYTE
- Entire www/ game package, including:
  campaign levels, daily experiments, timer, RP, hints, MoleCoin, story scenes,
  audio, SVG icons, account deletion, cloud systems and Apple Sign-In web logic.
- Approved resources/AppIcon-1024.png.
- Apple entitlement and Firebase iOS configuration.

TESTS PASSED
- Six game JavaScript syntax checks.
- Six Python build/verification script compilation checks.
- Package and Capacitor JSON parse.
- Codemagic YAML parse/required-step check.
- Approved icon: 1024x1024, opaque RGB.
- Synthetic generated Xcode project:
  two App configurations patched from iPhone+iPad to iPhone-only and verified.
- Synthetic signed IPA:
  UIDeviceFamily=[1] and correct bundle identifier verified.
- R27 vs R28 www/ byte comparison: no changes.
- ZIP integrity: checked after packaging.

IMPORTANT APP STORE CONNECT STEP
- Select the NEW R28 build after Apple finishes processing it.
- The old iPad-capable build will continue to request a 13-inch iPad screenshot.
- Confirm the real Moleculox icon appears under Included Assets before Add for Review.

LIMIT
- A real signed IPA and App Store processing cannot be completed in this local validation environment.
- Codemagic now contains fail-closed checks for both the generated project and the final signed IPA.

ZIP INTEGRITY: PASS
