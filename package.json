{
  "name": "moleculox-ios",
  "version": "8.5.54",
  "private": true,
  "description": "Moleculox iOS V8.5.54 R12 stability test: R8 auth/sync baseline, wHiTeWaY restricted to the verified Google account, Dr. E voice bank retained, proportional player/account UI, raised board, and larger in-game Dr. E.",
  "scripts": {
    "ios:prepare": "bash scripts/prepare-ios.sh",
    "ios:sync": "npx cap sync ios",
    "ios:open": "npx cap open ios",
    "check": "node --check www/js/game.js && node --check www/js/firebase.js && node --check www/js/sync-core.js"
  },
  "dependencies": {
    "@capacitor/core": "7.4.2",
    "@capacitor/ios": "7.4.2",
    "@capacitor-firebase/authentication": "7.3.1",
    "firebase": "11.10.0"
  },
  "devDependencies": {
    "@capacitor/cli": "7.4.2"
  }
}
